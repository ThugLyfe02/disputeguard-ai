import random


def generate_graph_embedding(node):

    # simplified embedding placeholder
    return [
        random.random(),
        random.random(),
        random.random(),
        random.random()
    ]